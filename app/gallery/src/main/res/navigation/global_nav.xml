<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/global_nav"
    app:startDestination="@id/dest_list">

    <fragment
        android:id="@+id/dest_list"
        android:name="com.lunacattus.app.gallery.feature.list.ListFragment"
        tools:layout="@layout/fragment_list">
        <action
            android:id="@+id/action_list_to_play"
            app:destination="@id/play_nav" />
    </fragment>

    <include app:graph="@navigation/play_nav" />

</navigation>